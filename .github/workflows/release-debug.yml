name: Build debug APK
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag for release (e.g. v0.9.0-beta10)'
        required: true
        default: 'v0.9.0-beta10'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK (v2)
        uses: android-actions/setup-android@v2
        with:
          api-level: 34
          build-tools: '34.0.0'

      - name: Ensure local.properties and settings.gradle.kts
        shell: bash
        run: |
          set -e
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          if [ ! -f android/settings.gradle ] && [ ! -f android/settings.gradle.kts ]; then
            {
              echo 'rootProject.name = "fight-calendar"'
              echo 'include(":app")'
            } > android/settings.gradle.kts
          else
            # fix accidental single-line file
            sed -i 's/ \+include(/\
include(/' android/settings.gradle.kts || true
          fi
          echo "----- settings.gradle.kts -----"
          cat android/settings.gradle.kts
          echo "--------------------------------"

      - name: Build debug APK (Gradle 8.7)
        uses: gradle/gradle-build-action@v3
        with:
          build-root-directory: android
          gradle-version: 8.7
          arguments: assembleDebug

      - name: Collect APK and SHA-256
        shell: bash
        run: |
          APK=$(find android -path "*/outputs/apk/debug/*.apk" -name "*.apk" | head -n 1)
          if [ -z "$APK" ]; then
            echo "::error::Debug APK not found. If your module name is not 'app', change the task to ':yourModule:assembleDebug'."
            exit 1
          fi
          echo "APK=$APK"
          mkdir -p artifacts
          cp "$APK" artifacts/app-debug.apk
          sha256sum artifacts/app-debug.apk | tee artifacts/SHA256.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: ${{ github.event.inputs.tag }}
          prerelease: true
          files: |
            artifacts/app-debug.apk
            artifacts/SHA256.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
